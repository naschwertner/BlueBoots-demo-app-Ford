import 'package:flutter/material.dart';

class MinhasTagsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Minhas Tags'),
      ),
      body: Center(
        child: Text('Aqui estão suas tags!'),
      ),
    );
  }
}
